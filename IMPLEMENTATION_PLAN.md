# 구현 계획 — RayNeo X3 Pro 배송 대기 중 (글래스-독립 선행)

> 전략: **글래스 없이 만들 수 있는 소프트웨어(에지 서버 + 폰 클라이언트 + Unity 토대)를 먼저 완성**하고,
> 글래스 도착 시 ①디바이스 리스크 2개 검증 ②표시 레이어만 포팅으로 마무리.
> 근거 = `RESEARCH_RAYNEO_X3.md` 5대 결정. SoT = `PATENT_IMPLEMENTATION.md`(A4).

## 아키텍처 (확정)
```
[카메라]            [에지(폰/PC)]                         [표시]
글래스 RGB(또는 폰) → YOLOv8-seg + A3 곡면워프 (우리 코드) → 좌표/메시 반환 → 글래스 웨이브가이드(또는 폰 화면)
            └ 무거운 연산은 글래스 밖 (AR1 약함·배터리 30분) — 특허 41/42 부합
```

---

## Phase 0 — 에지 오프로드 루프 (지금, 글래스 X) ⬅️ 시작
**목표: 폰 카메라 → PC가 인식·워프 → 폰이 곡면 디자인 렌더. (브라우저 ONNX 버그 우회 = 무거운 건 PC가)**
- [x] **0a. 에지 서버** (`web/edge_serve.py`) — HTTPS 정적 + `POST /infer`(JPEG→손톱 기하 JSON). 시작 시 모델 워밍업. PC 검증: 손톱3·73.7ms·정적서빙 OK.
- [x] 0b. 폰 클라이언트(`web/edge.html`+`edge.js`) — 프레임을 `/infer`로 전송, 받은 기하로 A3 엔진(`design_render.js`) 곡면 렌더. 추론/렌더 분리. **PC 검증 완료, 폰 실기 확인 대기.**
- [ ] 0c. 지연 은폐 — 검출 사이 트래킹/예측(왕복지연 가림), fps·지연 계측.
- **완료 기준:** 폰에서 손톱에 곡면 별이 ≥15fps·안정적으로(에지 추론) 뜸. ← 폰 테스트 1회 남음

## Phase 1 — Unity 토대 (지금~, 글래스 X)
**목표: 최종 코드베이스를 Unity로. 폰(Android)에서 먼저 완주.**
- [ ] 1a. Unity 설치 + RayNeo OpenXR Unity ARDK 셋업(문서대로, Unity 2020.3+).
- [ ] 1b. Unity 폰 앱: Camera2 프레임 → (에지 서버) → 결과 → **곡면 메시 워프 렌더**. Phase 0 로직을 Unity로.
- [ ] 1c. A3 워프를 Unity(C#/셰이더)로 이식 — `spec/warp_spec` 데이터 공유(py↔C#, 골든테스트).
- **완료 기준:** S22 Ultra에 깐 Unity 앱이 폰 카메라로 곡면 정합 동작(글래스와 동일 코드).

## Phase 2 — 글래스 도착 즉시: 디바이스 리스크 검증 (반나절, 1순위)
**목표: 데모 생사를 가르는 2개를 먼저 확인.** (실패해도 폴백 있음)
- [ ] 2a. **근거리 초점** — Camera2로 20~40cm 손톱 캡처 → 선명도/해상도/fps/지연 측정.
- [ ] 2b. **고정 안정성** — 가상 오브젝트가 손톱에 붙어있나(실사용 3DoF 확인).
- [ ] 2c. 표시 — 검은배경=투명 확인, 흰색 큰 영역 회피(전력/과열).
- **분기:** 통과 → 글래스 카메라로 캡처 / 실패 → **폰 카메라 캡처 + 글래스 표시** 폴백.

## Phase 3 — 글래스 표시 포팅 + 특허 시연
- [ ] 3a. Unity **표시 출력만** 글래스 ARDK로(캡처·연산은 Phase 0~1 그대로).
- [ ] 3b. 정합 캘리브 — 카메라↔디스플레이↔눈, ~2m 초점면 vs 손톱 ~30cm 오프셋 보정.
- [ ] 3c. **독립항 1·24·30 각 1회 시연 녹화** = 특허 실시 증거.

---

## 📦 글래스 오기 전 끝낼 체크리스트
- [ ] 에지 서버 동작(프레임→기하 JSON)
- [ ] 폰에서 곡면 워핑 확인(에지 경유)
- [ ] Unity 폰 빌드 성공 + 곡면 정합
- [ ] 디바이스 리스크 측정 앱/스크립트 준비(Camera2 근거리 캡처 측정)
→ 이게 다 되면 글래스 도착 시 Phase 2(반나절) + Phase 3만 남음.

## 🔧 글래스 오는 날 30분 안에 할 것
> **[2026-07-01 진행]** 안경 도착. RayNeo X3 확정, ADB 켜짐. adb/scrcpy 설치 완료.
> 막힘=Windows 복합USB 드라이버 → `tools/ar_helper/Fix-ADB-Driver-ADMIN.cmd`(관리자 1클릭) 준비됨.
> 초점 판정기·미러·사이드로드 헬퍼 전부 준비. 상세=[docs/AR_SETUP.md](docs/AR_SETUP.md) / [SESSION_2026-07-01](SESSION_2026-07-01_안경연결_보안툴킷.md).
1. ADB 연결 확정(드라이버 수정) → `AR-Helper.cmd info`로 칩셋/카메라 실측
2. `capture_focus_test.ps1`(2a 근거리 초점) + `fixation_test.ps1`(2b 고정) → 카메라 소스 확정
3. Android 테스트앱 빌드(JDK17+SDK) → `AR-Helper.cmd install` → 첫 렌더 / 이후 Unity 표시 레이어
