# 펜 가림 — A단계 진행 로그

> 2026-07-28 노트북 세션. 상위 계획은 [PEN_OCCLUSION_PLAN.md](PEN_OCCLUSION_PLAN.md).
> 브랜치 `feat/pen-occlusion`.

## 무엇을 했나

안경(RayNeo X3) 카메라로 **손 + 주황 나무 연필**(실제 브러시 대용)을 잡고 원본 프레임 200여 장을 수집한 뒤,
"펜이 있는 자리에서 디자인을 흐리는" 여러 방법을 **오프라인에서 직접 이미지로 검증**했다.

수집은 에지 서버에 넣은 `DUMP_DIR` 토글로 했다:

```powershell
$env:PEN_PROBE="1"; $env:DUMP_DIR="<폴더>"
.\.venv\Scripts\python.exe web\edge_serve.py     # 손+도구를 카메라 앞에 대면 원본 프레임이 쌓임
```

## 시도한 방법과 결과 (전부 실제 프레임으로 눈으로 확인)

| 방법 | 결과 |
|---|---|
| **색 단서** (어두움/저채도) | ❌ 연필이 **살구색≈피부색**이라 안 잡힘. 오히려 배경 어두운 옹이에 오탐 |
| **형태(직선) 검출** (Hough) | ❌ 배경 **문틀/몰딩 라인**이 연필보다 길고 강해서 그걸 잡음 |
| **손톱영역 이상치**(Lab 편차) | △ 연필 덮은 데는 잡지만, 손톱 하이라이트/가장자리로 **~25% 오탐 바닥**(640×400 저해상+작은 손톱) |
| **소프트 penDim** (편차 비례 흐리기) | ✅ **채택.** 하드 컷아웃의 구멍/깜빡임 없이 노이즈를 자연스럽게 흡수 |

## 핵심 결론

1. **도구를 검출하지 말고, "손톱 위에서 손톱이 아닌 픽셀"을 찾자.** 연필·브러시·금속 페룰 무엇이든
   손톱색과 다르기만 하면 잡힌다(도구 무관).
2. **하드 컷아웃(alpha=0) 대신 소프트 penDim**(편차에 비례해 알파를 낮춤). 작은 편차(하이라이트)는 거의
   안 흐려지고 큰 편차(도구)만 확실히 흐려져 → 구멍/깜빡임 없음. penDim 노브로 0(완전가림)~1(효과없음) 조절.
3. **이 연필은 최악 케이스**(살구색+얇음)라 효과가 미묘했다. 실제 도구는 **금속 페룰 + 검정/갈색 털**의
   네일 브러시 → 손톱색과 편차가 커서 **훨씬 잘 먹힐 것**. 최종 튜닝은 실제 브러시 프레임으로 해야 한다.

## 개념 증명 (전/후 비교)

가짜 디자인을 손톱에 얹고 `가림처리 없음` vs `소프트 penDim` 비교 → 연필이 손톱을 가로지르는 자리에서
디자인이 흐려져 연필이 비친다. 가림 없는 손톱엔 디자인 선명 유지.

- 아티팩트(이미지 3장): https://claude.ai/code/artifact/ea071e09-1238-4d63-9775-ab5398cc773e

## 코드 / 도구

- `web/edge_serve.py`
  - `_pen_probe_overlay()` — 단서 3종(어두움/저채도/에지) 모니터 오버레이. env `PEN_PROBE=0`으로 끔.
  - `DUMP_DIR` — 원본 프레임 수집 토글(A단계 데이터 모으기).
- `tools/pen_probe/analyze_frames.py` — **재사용 분석기.** 프레임 폴더 → YOLO 손톱세그 → 도구무관 가림가중치
  → 소프트 penDim 전/후 시뮬 저장. **브러시 오면 이거 한 줄만 다시 돌리면 됨:**
  ```powershell
  .\.venv\Scripts\python.exe tools\pen_probe\analyze_frames.py --frames <새폴더> --out <출력>
  ```
  파라미터: `LO/HI`(편차 정규화), `PEN_DIM`(잔여 불투명도), `BASE_A`(기본 불투명도).

## 다음

1. **[대기] 실제 네일 브러시** → 15초 캡처 → `analyze_frames.py` 재실행 → `PEN_DIM`/`LO,HI` 튜닝.
2. **[B단계] 온디바이스 셰이더** (재빌드 1회): 이 가림가중치로 디자인 알파를 곱한다.
   `NailOverlayRenderer`/`NailMeshRenderer` 양쪽에 적용. 마스크는 (a) 셰이더가 피드 직접 샘플 or
   (b) 서버가 준 폴리곤을 래스터화 — 둘 다 캔버스 공통변환이라 UV 일관.
3. **[병행] 폰 단독 구동 가능성** 조사 — 브러시 오기 전 노트북 세션에서 진행(별도 문서).
