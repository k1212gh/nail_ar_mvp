# 리서치 가이드 — 플랫폼/디바이스 (타겟: RayNeo X3 Pro AI+AR Glasses)

> **이 문서의 목적:** "어느 플랫폼·디바이스로 특허 PoC를 완성할지"를 정하기 위한 **리서치 설계도**.
> 그대로 deep-research에 넣거나, 사람이 항목별로 조사해 채우면 된다.
> 각 질문은 **[무엇을] / [왜 중요] / [결정에 미치는 영향] / [찾을 곳]** 형식.
> 작성 2026-06-24. 상위 맥락: `PATENT_IMPLEMENTATION.md`(SoT), `nail-ar-mvp-is-patent-poc`(메모리).

---

## 0. 한 줄 정의
공석태 특허 "착용형 증강현실 기반 정밀 작업 보조 시스템"의 **네일아트 실증기**. 카메라로 **실제 손톱을 인식**하고, 디자인을 **손톱 곡면에 유기적으로 정합(분절 메시 워핑)**해 **실시간 온디바이스**로 겹쳐 보여준다. **최종 타겟 = RayNeo X3 Pro AR 글래스 위 시연**(특허 "착용형" 요건).

## 1. 목적 (무엇을 이루려는가)
| 시점 | 목표 | 특허 매핑 |
|---|---|---|
| 단기 | 디자인을 실제 손톱 곡면에 정합(유기적), 실시간 | 청구항 1·8·10·11·19 |
| 중기 | 움직임 보정 / 가림 보정 / 대상 간 전사 + 폐루프 | 독립항 1·24·30, 14 |
| **최종** | **RayNeo X3 Pro 글래스 위에서 위 기능 시연** | "착용형" 요건, 청구항 1 |

**선행기술과 차별점 5가지(이걸 글래스에서 보여야 "특허 실시"):** ①실제 대상 위 실시간 안내(미리보기 아님) ②움직여도 위치 보정 ③가림 보정 ④폐루프(결과검출→다음/대칭) ⑤대상 간 전사.

## 2. 제반사항 (자산·제약 — 리서치의 전제)
| 항목 | 현황 |
|---|---|
| 타겟 글래스 | **RayNeo X3 Pro** — ✅실측(2026-07-01): 모델 ARGF20/MercuryLiteXR, **Qualcomm AR1(`neo`)**, **RAM ~4GB**, **Android 12(SDK32)**, arm64-v8a, 디스플레이 1280×480. 카메라 **id0 back 4032×3024(12MP) / id1 back 640×480**, fps 15/24/30, zoom 1–8×. |
| 폰 | 갤럭시 S22 Ultra (개발·중간 검증용) |
| PC | RTX 4070 SUPER 12GB (학습·ONNX·에지 연산 가능) |
| 우리 모델 | YOLOv8-seg 손톱 (.pt/.onnx) — **PC GPU서 18ms·검출 83% 검증됨** |
| 곡면 엔진 | A3(분절 메시 곡면 워핑) — PC 완성, 웹 렌더러 작성(실기기 미검증) |
| 보유 도구 | Python/torch/ultralytics ✅ · Android Studio ❌ · Unity ❌ |
| 알려진 신호 | Unity Sentis로 YOLOv8-seg는 **폰에서 ~5fps**(세그는 무거움) → 약한 글래스 칩 온디바이스 직행은 리스크 |

## 3. 왜 지금 리서치하나 (현 상태)
- PC `--seg yolo`: 실제 손톱 인식 + 곡면 정합까지 **작동 검증**(폰을 PC 연결 카메라로).
- 웹(onnxruntime-web): 폰 브라우저에서 **검출 0개(best 0.00)** + 3fps → 웹-ONNX 경로는 버그+성능 천장. 안경 경로도 아님.
- **타겟이 XREAL→RayNeo X3 Pro로 변경.** RayNeo는 독립형 안드로이드 AR이라 플랫폼·연산 그림이 완전히 다름 → 디바이스 사실관계부터 확정해야 길이 정해짐.

---

## 4. 핵심 리서치 질문 (그룹별, 우선순위 P0=결판/P1=중요/P2=참고)

### A. RayNeo X3 Pro 디바이스 사실관계 (P0 — 여기서 다 갈림)
- **A1 [독립형 vs 테더링]** X3 Pro는 자체 OS·칩으로 단독 구동인가, 폰 연결 필수인가? — *왜:* 연산을 글래스에서 할지 폰/PC로 보낼지 결정. *출처:* RayNeo 공식 스펙, 개발자 사이트, 리뷰(2025~2026).
- **A2 [칩/연산]** 칩셋(Snapdragon AR1 Gen1 등)·RAM·NPU/GPU? 온디바이스 ML 어디까지(NNAPI/LiteRT/QNN)? — *왜:* YOLOv8-seg를 글래스에서 실시간 돌릴 수 있는지의 핵심. *결정영향:* 못 돌리면 → 폰/PC 오프로드 아키텍처 필수(특허 41/42). *출처:* 스펙시트, Qualcomm AR1 문서, 개발자 벤치.
- **A3 [개발 플랫폼]** 앱을 무엇으로 만드나 — **Unity? 네이티브 Android? RayNeo 자체 SDK?** 지원 Unity 버전, XR 플러그인, AR Foundation 호환? — *왜:* Unity면 폰↔글래스 한 코드 + 곡면 3D 강점. 네이티브면 별도. *출처:* RayNeo Developer 포털, SDK 문서, GitHub 샘플.
- **A4 [카메라 접근]** 개발자가 **RGB 카메라 프레임을 실시간**으로 받을 수 있나? API·해상도·fps·지연·권한? 근거리(20~40cm) 손톱이 또렷이 잡히나(초점)? — *왜:* 특허 인식부의 절대 전제. 못 받으면 글래스 자체 인식 불가→폰 카메라 병용. *출처:* SDK 카메라 API 문서, 샘플코드, 개발자 포럼.
  - **✅실측 결론(2026-07-01):** ❌**서드파티 앱 카메라 차단.** 사이드로드 앱은 Camera2에서 `ERROR_CAMERA_DISABLED`(센서 프라이버시 OFF·appops allow에도 불변) → 시스템 앱(`com.leiniao.camera`)만 카메라 허용. 게다가 카메라는 **고정초점(~50cm+)**, 20~40cm 손톱은 SOFT. **→ 두 이유로 글래스 자체 캡처 불가. 아키텍처 확정: 폰(또는 PC) 캡처·인식 → 글래스는 표시 전용.** (계획서 Phase 2 폴백 분기 채택.)
- **A5 [정합/추적]** 6DoF/SLAM·평면/이미지/객체 추적 제공? **근거리 작은 곡면(손톱)에 가상 오브젝트를 안정 고정**할 수 있나? 디스플레이(웨이브가이드)·FOV·광학투과 여부? — *왜:* "손톱에 붙은 것처럼" 정합의 가능성. *출처:* SDK 추적 문서, 리뷰, 핸즈온.
- **A6 [앱 배포]** APK 사이드로드 가능? 개발자 모드? 스토어 정책? — *왜:* 우리가 실제로 올려 테스트 가능한지. *출처:* 개발자 가이드.

### B. 온디바이스 추론 가능성 (P0)
- **B1** 우리 YOLOv8-seg(.onnx)를 **글래스 칩에서** 실시간(≥10fps) 돌릴 수 있나? 경량화(YOLOv8n-seg, INT8 양자화, 입력 320/256↓) 시 어디까지? — *결정영향:* 가능→온디바이스, 불가→오프로드. *출처:* Sentis/LiteRT/QNN 벤치, AR1 ML 사례.
- **B2** 추론 엔진 선택: **Unity Sentis** vs **LiteRT(TF Lite)+NNAPI** vs **QNN(Qualcomm)** vs **ONNX Runtime Mobile** — 각 RayNeo/AR1 호환·속도. — *출처:* 각 엔진 문서 + AR1 지원표.

### C. 연산 배치 (P1 — 특허 청구항 41/42 직접 대응)
- **C1** "글래스 카메라 → 무거운 인식·세그는 **폰/PC(에지)**가 처리 → 결과 좌표만 글래스로 → 글래스는 표시" 아키텍처가 RayNeo에서 가능한가? 통신(Wi-Fi/BLE)·왕복 지연? — *왜:* 글래스 칩이 약해도 특허를 실증하는 합법적 우회(명세서 0341~0351). *출처:* SDK 통신/스트리밍, 일반 에지-AR 사례.

### D. 렌더링·정합 품질 (P1)
- **D1** 곡면 메시 워핑·법선 음영을 글래스에서 어떻게(Unity 3D면 기본). **광학 투과 디스플레이**에서 검은배경=투명 처리, 손톱 위 정합 오프셋 보정(눈↔디스플레이↔카메라 좌표계)? — *출처:* SDK 렌더/정합 문서.

### E. 대안 플랫폼 비교 (P1)
- **E1** **Unity vs 네이티브 Android**(RayNeo 기준): 공식 지원 강도, 추론·렌더·배포·반복속도·러닝커브. 우리 PC 자산(파이썬 엔진·WarpSpec)을 어느 쪽이 더 잘 재사용? — *결정영향:* 최종 코드베이스. *출처:* RayNeo 권장 워크플로 + 우리 코드 구조.
- **E2** 개발 순서: **폰(같은 엔진)에서 먼저 완성 → 글래스 포팅**이 RayNeo에서 성립하나(글래스 없이 개발 진척)? — *왜:* 글래스 1대로 병목 안 되게. 

### F. 특허 정합 체크 (P2)
- **F1** 독립항 1·24·30을 RayNeo에서 각 1회 시연하는 최소 경로 매핑(어떤 API로 무엇을). — *출처:* `PATENT_IMPLEMENTATION.md` + A~E 결과.

---

## 5. 이 리서치로 내릴 결정 (Decision Gate)
1. **연산 위치:** 글래스 온디바이스 vs 폰/PC 오프로드 (← B1/A2/C1)
2. **코드베이스:** Unity vs 네이티브 Android (← A3/E1)
3. **추론 엔진:** Sentis / LiteRT / QNN / ORT (← B2)
4. **카메라 소스:** 글래스 RGB vs 폰 카메라 병용 (← A4)
5. **개발 순서:** 폰 선개발→글래스 포팅 가능 여부 (← E2)
→ 5개가 정해지면 **구현 로드맵(PATENT_IMPLEMENTATION Phase A4)** 을 RayNeo 기준으로 다시 쓴다.

## 6. 원하는 산출물 형식 (리서치 결과는 이렇게)
- 각 질문에 **사실 + 출처 링크 + 신뢰도(확인/추정/불명)**. 공식 문서 > 리뷰 > 추정.
- 마지막에 **5개 결정 항목 권고 + 근거 1줄씩** + **미확인으로 남아 제조사/실물 확인이 필요한 것** 목록.

## 7. 참고 (기존 자산)
- **베이스라인 3대 질문(XREAL용이었지만 RayNeo에도 그대로 적용):** ①실시간 RGB 프레임 접근(fps/해상도/지연) ②근거리 20~40cm 손톱 선명 초점 ③작은 곡면 객체 안정 추적. → A4/A5와 동일.
- 특허 원문: `ai안경증강현실작업보조시스템.pdf` (청구항 44, 연산배치 41·42, 곡면 10·11).
- 우리 검증된 모델·엔진은 그대로 이식 자산(플랫폼만 바뀜).

## 8. 비-목표 (이번 리서치 범위 밖)
- 새 손톱 모델 학습(이미 충분), 웹-ONNX 디버깅(안경 경로 아님이라 보류), 글래스 구매 의사결정 자체(기능 확인이 먼저).
