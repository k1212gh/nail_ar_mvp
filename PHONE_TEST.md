# 폰으로 코어 파이프라인 테스트하기 (Windows)

특허(착용형 AR 정밀 작업 보조)의 **코어 CV 파이프라인을 폰 카메라로 검증**하는 절차다.
폰은 특허의 "비디오 투과형 + 스마트폰 연산" 실시예(명세서 0102/0109, 도 16-B) 역할을 하며,
인식·안내·정합·표시를 모두 PC에서 처리한다. 글래스(XREAL)는 나중에 입력 소스만 교체한다.

---

## 0. 준비 (한 번만)

```powershell
cd <프로젝트 폴더>\nail_ar_mvp
python -m pip install opencv-python numpy mediapipe
python tools\smoke_test.py        # 4/4 PASS 떠야 정상 (모델 자동 다운로드)
```

> torch/ultralytics는 코어 검증에 필요 없다(설치하지 말 것). 첫 실행 때 손 모델이
> `models\hand_landmarker.task`로 자동 다운로드된다.

---

## 1. 폰 → PC 연결: 두 가지 방법 중 택1

### 방법 A. 가상 웹캠 (가장 간단, 권장)
폰을 PC의 "웹캠"으로 인식시키는 앱을 쓴다. **Iriun Webcam** 또는 **DroidCam**.

1. 폰과 PC에 같은 앱을 설치하고 **같은 Wi-Fi**에 연결(또는 USB).
2. 폰 앱 실행 → PC 앱이 폰 화면을 받으면 연결 완료.
3. 카메라 번호 확인:
   ```powershell
   python tools\list_cameras.py
   ```
   → 사용 가능한 인덱스가 출력된다(보통 마지막 번호가 폰).
4. 실행:
   ```powershell
   python main.py --source 1 --bench
   ```
   (1 자리에 위에서 찾은 번호. 내장캠이 없으면 보통 0)

### 방법 B. IP Webcam 스트림 URL (드라이버 불필요)
Android **IP Webcam**(Pavel Khlebovich) 앱이 가장 안정적이다.

1. 앱 설치 → 맨 아래 **"Start server"**.
2. 화면에 뜨는 주소 확인, 예: `http://192.168.0.12:8080`
3. PC 브라우저에서 `http://192.168.0.12:8080/video` 가 열리는지 먼저 확인(연결 점검).
4. 실행:
   ```powershell
   python main.py --source http://192.168.0.12:8080/video --bench
   ```

> DroidCam URL 방식을 쓸 경우: `http://<폰IP>:4747/video`

---

## 2. 잘 잡히게 찍는 법 (근거리 핵심)
손톱은 작고 가까워서(15~25cm) **초점·조명·구도**가 결과를 좌우한다.

- **초점**: 앱에서 손톱을 탭해 초점 고정(IP Webcam은 화면 탭). 흐리면 인식률이 급락한다.
- **조명**: 밝고 균일하게. 그림자·강한 반사(젤네일 광택)는 마스크를 망친다.
- **구도**: 손 전체가 화면의 절반 이상을 채우게. 손등이 보이는 각도가 손가락 인식에 유리.
- 떨림이 크면 손을 받치거나 폰을 거치한다.

---

## 2-b. 별 디자인 입히기 테스트 (핵심 실험)
샘플 이미지를 손톱에 자연스럽게 올리는지 보는 테스트.

```powershell
python tools\make_star.py                                  # samples\star.png 생성(1회)
python main.py --source 1 --design samples\star.png --bench
```
- 디자인을 주면 가이드선은 자동으로 꺼지고 **별만** 손톱에 합성된다(가이드도 같이 보려면 `--keep-guides`).
- 크기 조절: `--design-scale 0.7` (손톱 대비 0~1)
- 단축키 `d` = 디자인 on/off. 다른 이미지도 됨: `--design 내그림.png`(투명 PNG 권장)
- 체크포인트: 손을 돌리면 별이 손톱을 **따라 돌고**, 손톱 밖으로 **안 삐져나가는지**.

## 3. 화면에서 보이는 것 / 단축키
- 초록 = 세로 중심선 + 가로 보조축, 주황 = 격자, 자홍 = 프렌치 라인, 빨강 = 부착점
- 좌상단 HUD = fps·검출 손톱 수 / 우상단 = `--bench` 계측
- 단축키: `q` 종료 · `m` 마스크 · `g` 격자 · `f` 프렌치 · `l` 랜드마크

---

## 4. 합·불 판정 (코어 게이트)
`--bench`로 켜면 종료 시 콘솔에 요약이 나온다:

```
=== 검증 요약 (코어 파이프라인) ===
  fps        : 평균 18.2  최저 11.0   (목표 평균>=15)
  검출률      : 86%   (목표>=80%)
  손톱/프레임 : 4.3
  떨림(jitter): 2.1 px   (낮을수록 안정)
```

- **fps 평균 ≥ 15**, **검출률 ≥ 80%**, **떨림이 시각적으로 거슬리지 않으면** 코어 통과.
- 프레임별 원자료가 필요하면: `--bench-csv bench.csv` 추가.

### 자주 막히는 곳
| 증상 | 원인/해결 |
|---|---|
| `입력을 열 수 없습니다` | 카메라 번호 틀림(`list_cameras.py`) 또는 URL 오타/다른 Wi-Fi |
| 손톱이 안 잡힘 | 초점 흐림, 손이 너무 멀거나 작음 → 더 가까이·밝게 |
| 마스크가 손톱을 못 따라감 | 광택/그림자 → 조명 개선, 또는 `--seg color`로 비교 |
| fps가 너무 낮음 | 해상도 낮추기 `--width 960 --height 540`, `--redetect 8` |
| 떨림이 큼 | `--redetect` 키우거나(재검출 줄임) 손 고정 |
