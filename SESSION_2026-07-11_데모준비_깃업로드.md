# 세션 기록 — 2026-07-10~11 · 사장님 데모 준비 + 깃허브 업로드

> 이전 세션: [SESSION_2026-07-01](SESSION_2026-07-01_안경연결_보안툴킷.md) · 안경 앱 결과: [docs/GLASSES_APP_RESULTS.md](docs/GLASSES_APP_RESULTS.md)
> 이 문서는 사용자 요청("전체 대화내역까지 포함해서 깃허브에 올려라")에 따라 이번 세션의 논의·결정을 정리한 것.

---

## 1. 발단 — "내일 사장님한테 외부에서 보여줘야 하는데 어떻게?"

사용자 상황: 내일(2026-07-11) **외부 장소에서 사장님에게 네일-AR MVP를 시연**해야 함. "외부에 나가야 하니" 어떻게 보여줄 수 있을지 고민.

### 핵심 진단 (안심 포인트)
- 이 시스템은 **인터넷이 필요 없음.** 전부 노트북 내부(localhost) 또는 USB 케이블(`adb reverse`)로만 동작.
- 외부 데모가 깨지는 원인은 "연결"이 아니라 **"현장 세팅 실수"** → 관건은 **리허설과 백업**.

## 2. 시연 가능한 3가지 카드 (안정성 순)

| 순위 | 방식 | 필요 장비 | 특징 |
|---|---|---|---|
| 🟢 1 | **노트북 매직미러** | 노트북(웹캠) | `edge_serve.py` + 브라우저. 화면 커서 사장님 보기 좋음. 오프라인. |
| 🟢 1 | **폰 APK 라이브** | 안드로이드 폰 | 후면 카메라, **초점 제일 좋고** 자체완결형이라 제일 안 깨짐. |
| 🟡 2 | **실물 안경** | 노트북+안경+USB | 임팩트 최고. ADB 드라이버·Doze 절전·**근시 흐림** 리스크 있음. |
| 🟢 백업 | **사전 녹화 영상** | 폰 | 뭐가 터져도 100% 방어. 프로 데모의 국룰. |

## 3. 사용자 확정 사항

- **들고 갈 장비:** RayNeo X3 안경 + USB케이블, **노트북, 안드로이드 폰** (전부 보유).
- **데모 목적:** **"기술이 진짜 된다"는 증명** → 안정성 최우선, 안경 임팩트는 보너스.

### 확정 전략 — 3막 구성
- **0막(백업):** 오늘 밤 매직미러/폰 돌아가는 화면을 30초 녹화 → 폰 저장.
- **1막(메인, 안정 라이브):** 노트북 매직미러 **또는** 폰 APK 라이브 중 더 잘 되던 것으로, 사장님이 **직접 손을 대보게**.
- **2막(와우, 보너스):** 기술 증명 끝난 뒤 안경 껴보게 → 안 떠도 데모는 이미 성공.

## 4. GPU 질문 — "노트북에 외장 GPU 없고 내장뿐인데 괜찮나?"

**결론: 문제 없음.**
- **인식 = CPU (MediaPipe 랜드마크).** 에지 서버 기본 모드가 `hands`([web/edge_serve.py:58](web/edge_serve.py#L58)). MediaPipe는 CPU 실시간이 정상. 안경(모바일칩)에서도 537ms에 손톱 5개 검출했으니 노트북 CPU는 훨씬 빠름.
- **렌더 = 가벼운 WebGL2.** 인텔/AMD 내장 GPU 전부 지원. 곡면 메시 몇 개라 연산량 작음.
- **주의:** `NAIL_MODE=yolo`로 켜지 말 것(CPU에서 무거움). 기본값 `hands` 유지.
- **안전망:** WebGL2 실패 시 canvas2D 평면 렌더 자동 강등. 폰·안경 경로는 노트북 GPU와 무관.

## 5. 오늘 밤 리허설 체크리스트

- [ ] 노트북: 들고 갈 그 기계에서 `python web/edge_serve.py` → `https://localhost:8443/edge.html` → 인증서 경고 1회 통과 → 손 인식 확인. (다른 노트북이면 저장소+`models/` 복사 후 `pip install -r requirements.txt`)
- [ ] 폰: APK 설치 + 후면 카메라 검출 확인 (`AR-Helper.cmd install ...app-debug.apk`)
- [ ] 안경: `adb devices`에 `device`로 뜨는지 → `run_glasses_demo.ps1 -Mode network` 성공
- [ ] 백업 영상 녹화 → 폰 저장
- [ ] 충전(노트북·안경·폰) + USB 케이블(안경·폰) 챙기기
- [ ] 밝은 조명 자리 확보

## 6. 깃허브 업로드 (이 세션 후반 작업)

사용자 요청: **"전체 대화내역까지 포함해서 깃허브에 레포 파서 올려라."**

### 업로드 전 안전 점검 결과
- **시크릿 스캔: 깨끗.** 걸린 4개 파일(telegram_notify.py 등)은 전부 플레이스홀더/설명(`<BOT_TOKEN>`, 환경변수)뿐. 실제 토큰·비번 없음.
- **실제 민감물:** `certs/key.pem`(개인키) → gitignore로 제외. `telegram_config.json`(실토큰 저장처) → 존재 안 함 + 방어적 제외.
- **대용량 제외 대상:** `Nail/Library`(2.9G), `_archive`(1.9G), 빌드산출물, `*.apk`(Fennec 119M 등), `*.zip`(RayNeo 런타임 28M — 독점물), 로그, 임시 이미지.
- **IP 민감:** `ai안경증강현실작업보조시스템.pdf`(특허), `PATENT_IMPLEMENTATION.md` → **비공개(private) 저장소일 때만 포함.**

### 결정
- 저장소 공개범위: **private 강력 권장** (특허 IP + 사업 맥락 대화기록 포함).
- 인증: **Git Credential Manager** 존재 → push 시 브라우저 로그인 1회.
- `gh` CLI 미설치 → 원격 저장소는 사용자가 GitHub에서 빈 repo 생성 후 URL 제공, 또는 `gh` 설치.

### 실행 결과
- gh CLI winget 설치 → 디바이스플로우 로그인(k1212gh) → **private repo `k1212gh/nail_ar_mvp` 생성+push 완료.** 823파일/198M. 시크릿·대용량 제외. 참고: [[github-repo-private]].

## 7. 안경 데모 라이브 프리플라이트 (데스크톱, 2026-07-11)

사용자 정정: **"안경으로 해야 돼, 원래 테스트하던 거"** → 데모는 Unity 안경 앱(`com.DefaultCompany.NailMesh` 등, 안경에 이미 설치됨). 네이티브 `com.example.nailar`는 미설치·미사용.

**실제 구조:** 안경 앱이 카메라 프레임을 PC 에지서버(`edge_serve.py`, HTTPS:8443)로 **USB(`adb reverse tcp:8443`)** 전송 → PC가 MediaPipe로 검출 → 안경이 곡면 메쉬 렌더. WiFi 불필요.

**검증 결과 (지금 실동작):**
- 안경(006A5E5038F3297 = ARGF20)이 에지서버로 **~15-20fps 프레임 전송**, 검출 **9ms/프레임**. 로그 `infer bytes=20700 nails=0 ms=9`(손 없어서 0). = 안경↔PC end-to-end 파이프라인 **살아있음 확인.**
- 앱 포그라운드 정상(`UnityOpenXrActivity`), 카메라 권한 부여됨, 스테레오 렌더(캡처=검정+시안점2 = 투과 디스플레이 정상).

**내일 리스크 & 해결 (노트북 이관):**
- 이 검증 PC = **RTX 4070 SUPER 데스크톱** ≠ 가져갈 노트북(내장GPU). → 노트북에 셋업 재현 필요. **단 실시간 검출은 MediaPipe CPU라 내장GPU로 충분**(YOLO는 CUDA없으면 자동 CPU: [yolo_nail_seg.py:31](src/yolo_nail_seg.py#L31)).
- **함정 발견·수정:** `requirements.txt`가 불완전(에지서버가 쓰는 `ultralytics`·`cryptography` 누락 → 노트북 pip install 시 서버 안 켜짐). → requirements.txt 보강 커밋.
- 노트북 준비물 신설: [docs/DEMO_RUNBOOK.md](docs/DEMO_RUNBOOK.md)(단계별 실행서) + [tools/laptop_setup.ps1](tools/laptop_setup.ps1)(자동 점검/설치). 인증서·모델은 자동생성/깃포함이라 turnkey.
- **다음: 노트북에서 laptop_setup.ps1 완주 + 안경 물려 리허설.**
