{
  "name": "nail-platform",
  "private": true,
  "version": "0.0.0",
  "description": "네일샵 통합 관리 플랫폼 (백오피스 + 중계제어 + AR연동). docs/BACKOFFICE_PLATFORM_PLAN.md 참조.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.0.14",
    "typescript": "^5.5.4"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@prisma/client", "@prisma/engines", "prisma", "esbuild", "electron"]
  }
}
