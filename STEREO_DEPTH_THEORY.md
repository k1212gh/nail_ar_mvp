# 스테레오 깊이 측정 이론 — RayNeo X3 Pro 듀얼 카메라 적용 가능성 분석

> 목적: "AR 글래스에 카메라가 2개니까 스테레오로 손톱 깊이/기울기를 구하면 되지 않나?"
> 라는 질문에 대한 **이론적 근거 정리**. 결론은 "이 기기에선 비현실적, 단안 RGB + 랜드마크 z가 합리적".
> 특허 PoC 리서치 근거 / 기술 의사결정 기록용.
>
> 관련 문서: [RESEARCH_RAYNEO_X3.md](RESEARCH_RAYNEO_X3.md) · [PATENT_IMPLEMENTATION.md](PATENT_IMPLEMENTATION.md) · [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md)

---

## 0. 기기 전제 (RayNeo X3 Pro 카메라 구성)

- **카메라 ①**: Sony IMX681, 12MP **컬러(RGB)** — 사진/영상용
- **카메라 ②**: OmniVision 계열 **흑백(mono) 공간(spatial)/SLAM 카메라** — 시스템 트래킹용
- 두 카메라는 **매칭된 스테레오 쌍이 아님**(이종: 컬러+흑백, 해상도·FOV·용도 다름)
- **개발자 접근**: RGB 1대만 Android `Camera2`로 접근 가능. SLAM 카메라 raw 프레임은 **OS 전용(앱 미노출)** — 이게 상위 1번 벽
- 시스템은 두 카메라로 내부 SLAM(6DoF·평면검출)을 돌리지만, 앱엔 그 *결과*만 줌(손톱용 픽셀 깊이맵 ✕)

---

## A. 출발점 — 스테레오는 왜 캘리브레이션이 *반드시* 필요한가

스테레오 깊이의 전부는 이 한 줄(삼각측량):

```
Z = f · B / d
```

- `Z` = 물체까지 깊이 (구하려는 값)
- `f` = 초점거리 (픽셀 단위)
- `B` = baseline (두 카메라 광학중심 사이 거리)
- `d` = disparity (같은 점이 좌·우 영상에서 가로로 어긋난 양, 픽셀)

핵심: **`f`, `B`를 모르면 `d`를 구해도 `Z`로 못 바꿈.** 그리고 `d`를 구하려면
"왼쪽 영상의 이 점 = 오른쪽 영상의 어디?"(대응점 매칭)를 풀어야 하고,
그 탐색을 가능케 하는 게 rectification → 둘 다 캘리브레이션에서 나옴.

---

## B. "캘리브레이션 미공개"가 정확히 뭘 말하는가

### ① 내부 파라미터 (Intrinsics) — 카메라 *각각*
픽셀 ↔ 3D 광선(ray) 변환표. 행렬 `K`:

```
      [ fx   0   cx ]
K  =  [  0  fy   cy ]
      [  0   0    1 ]
```
+ 왜곡계수 `(k1, k2, p1, p2, …)` (렌즈 배럴/핀쿠션 왜곡).

`K`를 모르면 "이 픽셀이 공간의 어느 방향을 보는가"를 못 정함 → 광선을 못 쏘니 삼각측량 불가.

### ② 외부 파라미터 (Extrinsics) — 두 카메라 *사이의 관계*
한 카메라 기준 다른 카메라의 **회전(R)·이동(t)**:

```
R = 3×3 회전행렬,   t = 3×1 평행이동벡터
baseline B = |t|
```

이 `R, t`가 바로 **"정합행렬"의 본체**. 이걸로 두 시점을 하나의 좌표계로 묶음.

> **"미공개"의 의미**: RayNeo는 `K1, K2, 왜곡, R, t`를 공개하지 않음(공장 측정값을 SLAM 내부에만 사용).
> 숫자를 모르니 → **직접 측정(자가 캘리브)** 필요 (E절).

---

## C. 에피폴라 기하와 Rectification — 왜 "정합행렬"이 필수인가

### 에피폴라 제약 (Epipolar constraint)
왼쪽 영상의 점 `p`에 대응하는 오른쪽 점은, 오른쪽 영상 전체가 아니라
**하나의 직선(epipolar line)** 위에만 존재. 이 관계를 담은 게 fundamental 행렬 `F`:

```
p_right^T · F · p_left = 0
```
`F`는 `K1, K2, R, t`로 계산됨 → 캘리브 없으면 `F`도 없음.

### Rectification (영상 정렬)
매칭을 쉽게 하려고 두 영상을 **에피폴라 선이 수평·같은 높이가 되도록** 워핑하는 단계:

```
정렬 전: 대응점이 비스듬한 선 위 어딘가 (2D 탐색)
정렬 후: 같은 y 행(row)에서만 좌우 이동 (1D 탐색) → d = xL - xR
```

이게 되어야 `d`를 "같은 줄에서의 가로 차이"로 단순 정의 가능.
Rectification 변환 계산엔 정확한 `K1,K2,R,t` 필요 → 다시 캘리브레이션.
정렬이 1픽셀이라도 어긋나면(세로 오정렬) 매칭이 엉뚱한 줄을 뒤져 깊이가 통째로 틀림.

---

## D. 이종 카메라가 Rectify를 "극상 난이도"로 만드는 4가지

RayNeo의 두 카메라 = **컬러 12MP RGB + 흑백 저해상 SLAM** — 짝이 안 맞음.

### ① 초점거리·FOV 다름 → 각해상도(angular resolution) 불일치
SLAM캠은 광각(트래킹용), RGB는 좁고 고해상. 같은 손톱이 두 영상에서 **다른 픽셀 크기**로 찍힘.
- Rectification이 두 영상을 공통 평면에 재투영하는데 FOV 차가 크면 한쪽을 심하게 늘리거나 줄여야(resample) 함 → 보간 아티팩트·디테일 손실.
- 겹치는 영역(overlap)이 좁아져 **손톱이 두 영상에 동시에 잘 잡힐 보장**이 약함(근거리에서 시차로 한쪽 밖으로 벗어남).

### ② 해상도 다름 → 샘플링 불일치
12MP vs 저해상. 공통 해상도로 리샘플 필요 → 고해상 정보 버리거나 저해상 업샘플(가짜 디테일).
서브픽셀 매칭 정밀도가 저해상 쪽에 발목 잡힘.

### ③ 셔터 방식 다름 (rolling vs global) + 비동기
- **Rolling shutter**: 영상을 위→아래 줄 단위 시간차 노출. 두 카메라 줄 타이밍이 다르면 **움직이는 손**이 서로 다르게 일그러짐(skew) → 같은 점인데 위치 어긋남 → disparity 오염.
- **비동기(sync 없음)**: 두 프레임이 다른 *시각*에 찍히면 손의 미세 움직임이 그대로 가짜 disparity가 됨. 하드웨어 동기(트리거 공유) 없으면 동적 물체엔 치명적.

### ④ 흑백 vs 컬러 → 광도(photometric) 불일치
스테레오 매칭은 "두 영상에서 **밝기 패턴이 비슷한** 곳"을 대응시킴.
- 컬러→흑백 변환은 색은 맞춰도 **분광 감도(spectral response)가 다름**. 모노 SLAM캠은 보통 **근적외선(IR)에 민감**하거나 다른 감마/노출 → 같은 표면이 두 영상에서 밝기가 다름.
- 매칭의 기본 가정(밝기 항상성, brightness constancy)이 깨짐 → 오대응(outlier)·구멍(hole) 급증.

> 사용자 아이디어("둘 다 흑백처리")는 ④의 *색* 불일치만 완화. ①②③과 **상위 벽(raw 접근 불가)**은 그대로 남음.

---

## E. 자가 캘리브레이션 — 절차와 왜 "별도 프로젝트"인가

공개 안 됐으니 직접 측정. 표준 = **Zhang의 방법(체커보드)** + **스테레오 캘리브**:

1. 알려진 패턴(체커보드, 칸 크기 정확히 앎)을 **두 카메라로 동시에** 여러 각도·거리에서 20~40장 촬영.
2. 각 영상 코너 검출 → `K1, 왜곡1` / `K2, 왜곡2` 추정(단안 캘리브 ×2).
3. 두 영상 코너 대응으로 **스테레오 캘리브** → `R, t` 추정.
4. 재투영오차(reprojection error)로 품질 검증(보통 < 0.5px 목표).

### 이 기기에서 특히 어려운 이유
- **동시 촬영 보장 난이도**: 비동기·셔터차로 "정확히 같은 순간"의 코너가 안 맞음 → `R,t` 오염(정적 패턴+삼각대로 완화는 가능하나 번거로움).
- **이종 검출**: 패턴이 저해상/IR 쪽에서 코너가 흐림 → 검출 정확도 저하 → 외부파라미터 오차.
- **raw 접근부터 막힘**: B/C절 데이터(두 카메라 raw 프레임)를 앱이 못 받음 → 이게 진짜 1번 벽.

→ 캘리브는 **접근이 뚫린 뒤에야 시작하는, 그 자체로 수 주짜리 프로젝트.**

---

## F. 오차 전파 + 손톱은 스테레오의 "최악 타깃"

### 깊이 오차 전파 공식
disparity 측정오차 `Δd`가 깊이오차 `ΔZ`로 번지는 양 (`Z = fB/d` 미분):

```
ΔZ = (Z² / (f · B)) · Δd
```

- 깊이오차는 `Z²`에 비례(멀수록 급증), `B`에 반비례(baseline 클수록 정확).
- 근거리(Z=0.3m)는 `Z²`가 작아 **기하학적으론 오히려 유리**(disparity가 큼).
  - ※ 초기 메모의 "근거리라 오차 증폭"은 부정확 — 정정. 근거리 자체는 유리.
- **진짜 문제는 `Δd`(매칭/캘리브 오차)와 작은 `B`**: 안경 baseline은 수 cm로 작고,
  캘리브가 부정확하면 `Δd`에 **계통오차(bias)**가 실려 `ΔZ`가 통째로 치우침.

### 손톱이라는 타깃의 저주
스테레오 매칭은 텍스처(무늬)가 있어야 대응점을 찾는데, 손톱은:
- **저텍스처(textureless)**: 매끈해 매칭 특징이 거의 없음 → disparity 모호.
- **정반사(specular)**: 광택 하이라이트가 **시점마다 다른 위치**에 생김 → 같은 점인데 좌·우에서 다르게 빛남 → 가짜 대응(brightness constancy 위반의 극단).
- **얇고 작음**: 차지 픽셀 적음 → 서브픽셀 오차 비중↑.

→ 텍스처 없고 정반사하는 작은 곡면 = 스테레오 매칭이 가장 못 푸는 케이스.

---

## G. 정리 (한 장)

| 단계 | 필요한 것 | 이 기기에서의 벽 |
|---|---|---|
| 픽셀→광선 | `K1,K2`+왜곡 | 미공개 → 자가측정 |
| 두 시점 결합 | `R,t` (정합행렬) | 미공개 → 스테레오 캘리브 |
| 매칭 1D화 | rectification | FOV·해상도·셔터·분광 다 달라 정렬 깨짐 |
| disparity→깊이 | `f,B` + 정확한 `d` | 저텍스처·정반사 손톱 → `d` 자체가 부정확 |
| 그 전에… | 두 카메라 raw 접근 | **앱이 SLAM캠 프레임 못 받음(최상위 벽)** |

**핵심 한 줄**: 깊이는 카메라 개수가 아니라 **두 카메라의 정확한 기하 관계(캘리브)+정렬+매칭**에서
나오는데, 이 기기는 그 재료(파라미터·raw 접근)가 다 막혀 있고, 설령 다 뚫어도 손톱은
스테레오로 풀기 가장 어려운 표면. → **단안 RGB + 랜드마크 z**가 비용 대비 압도적으로 합리적.

---

## H. 그래서 우리의 선택 (단안 + 랜드마크 z)

- 손톱 기울기/깊이 단서는 **MediaPipe 손 랜드마크의 z(상대깊이)**에서 추출.
  - tip/dip 관절의 z 차 → out-of-plane 기울기 추정 (구현: [web/app.js](web/app.js) `nailsFrom()`, `tiltDeg`).
- RGB 1대만으로 동작 → 스테레오 캘리브·동기·raw 접근 전부 불필요.
- 글래스에서도 동일하게 RGB + 랜드마크로 이식(Unity 단계).

### 미확정 / 후속 확인 (낙관 금지)
- RayNeo SDK가 **깊이/스페이셜 메시 API**를 노출하는지 문서 확인 필요. 노출돼도 손톱 크기엔 거칠 가능성 큼 → "없다 치고" 설계가 안전.
- 도착 시 **카메라 나열 진단**(`CameraManager.getCameraIdList()`)으로 2번 카메라 잠김 여부 사실 확인.
- 필요 시 RayNeo 개발자에 트래킹캠 raw / 깊이 API 노출 공식 문의(루팅 없이 합법 경로).
