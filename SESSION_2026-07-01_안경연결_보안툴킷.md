# 세션 2026-07-01 — 안경 도착·연결 + 보안 툴킷(Tvini 대체)

> 안경(RayNeo X3) USB 연결 착수 + Tvini 3대 기능을 오픈소스로 재구현.
> 대부분 자율 세션(사용자 부재 1h)으로 진행. 통합 가이드: [docs/AR_SETUP.md](docs/AR_SETUP.md).

## TL;DR
- **RayNeo X3 확정** (USB VID_18D1&PID_4EE2). **ADB는 켜짐**(Windows에 `ADB Interface` 열거됨).
- **연결 블로커 = Windows 복합장치 USB 드라이버**. adb(libusb)가 ADB 엔드포인트를 못 엶
  (`ADB interface missing endpoints`). macOS엔 없는 Windows 전용 문제.
  → **수정 준비 완료**: `tools/ar_helper/Fix-ADB-Driver-ADMIN.cmd` (관리자 1클릭, 다운로드 없음).
  ⛔ 사용자 UAC 승인 필요 → 부재중이라 대기.
- **Tvini 3종 → 오픈소스·로컬로 재구현 완료**(기기/관리자 불요 범위까지):
  1. APK설치+미러 = **scrcpy/adb** 헬퍼(`AR-Helper.cmd`) — 빌드·자체검증 완료
  2. 원격 데스크톱 = **Sunshine+Moonlight** 설치 스크립트+가이드
  3. 폰→안경 = **ScreenStream**(LAN 웹서버)→안경 브라우저 — 문서화
- **초점/도수**(조사): X3 가상화면 ~4m 고정, 디옵터 다이얼 없음 → **자석 도수 인서트(원용)** 또는 콘택트.
- **PC 엔진 회귀 5/5 PASS**(UTF-8). parity 최대오차 0.00e+00. 엔진 건강.

## 한 일 (시간순)
1. `winget install Genymobile.scrcpy` (adb 번들, 해시검증). 경로 자동해석 헬퍼.
2. `adb devices` 빈 목록 → 진단: Windows는 안경을 MTP로 봄 = 케이블 OK, 디버깅만 off.
3. 사용자가 안경에서 ADB 토글(스와이프10회) → `ADB Interface`(Class ff/42/01) 열거 확인 = **ON 확정**.
4. 그래도 adb 목록 빔 → 서버 트레이스로 근본원인 규명: **복합장치+libusb missing-endpoints**.
5. 드라이버 수정 스크립트(ADB GUID 레지스트리 등록) + 자가승격 런처 준비.
6. **Task2-1** 보안 헬퍼 완성: 기기목록/미러/APK설치/녹화/WiFi/정보. doctor·devices·문법 검증.
7. **Task2-2** Sunshine 자가승격 설치+LAN전용 방화벽 스크립트. winget ID 검증(`LizardByte.Sunshine`).
8. **Task2-3** ScreenStream 경로 문서화(+PC경유 대안).
9. **초점 리서치** + **focus_analyze.py**(선명도 지표, selftest PASS: 선명92701 vs 흐림6.3) + 캡처 오케스트레이터.
10. **fixation_test.ps1**(scrcpy 녹화 기반 3DoF 드리프트 검토).
11. PC 회귀 5/5 PASS(UTF-8). 문서/메모리 갱신.

## 알아낸 사실 (신뢰도)
- **[확인]** 기기=RayNeo X3, USB 복합(MTP MI_00 + ADB MI_01), 시리얼 006A5E5038F3297.
- **[확인]** ADB on 시 Windows가 WinUSB로 자동바인딩하나, adb libusb 백엔드가 복합장치
  ADB 인터페이스 엔드포인트 접근 실패 → 레거시(AdbWinApi) 백엔드가 인식하려면 장치에
  **ADB 인터페이스 GUID `{F72FE0D4-...}` 등록** 필요.
- **[확인]** 프로젝트 PC 테스트는 cp949 콘솔에서 유니코드 print로만 죽음(로직 정상). UTF-8로 전부 PASS.
- **[조사]** X3 Pro: 가상상 ~4m 고정, 도수 인서트 근시0~-10D/난시0~-2D 단초점 원용, 안경위착용 비권장.

## 다음 (사용자 복귀 시)
1. **`tools/ar_helper/Fix-ADB-Driver-ADMIN.cmd` 더블클릭 → UAC 예** → `adb devices` 확인
   (unauthorized면 안경 팝업 수락). 실패 시 WiFi ADB 폴백.
2. `AR-Helper.cmd info`로 칩셋/카메라 실측 → `RESEARCH_RAYNEO_X3.md` A2/A4 추정→사실 갱신.
3. `capture_focus_test.ps1`로 근거리 초점(ⓐ) 판정 → 카메라 소스 확정.
4. Android 테스트앱 빌드(JDK17+SDK, docs/AR_SETUP §8) → `AR-Helper.cmd install` → 첫 렌더 + fixation(ⓑ).

## 신규 파일
- `tools/ar_helper/` : adb_env, ar_helper(+AR-Helper.cmd), device_info, watch_device,
  fix_adb_driver(+Fix-ADB-Driver-ADMIN.cmd), setup_remote_desktop, focus_analyze.py,
  capture_focus_test, fixation_test, verify_apk, first_connect, setup_android_build.
- `tools/build/` : setup_buildkit, build_app, deploy_app, gen_nail_design, frame_server, run_glasses_demo.
- `tools/notify/` : telegram_notify + NOTIFY_SETUP.
- `docs/AR_SETUP.md`(셋업/보안/초점) · `docs/GLASSES_APP_RESULTS.md`(앱 개발 결과) · `START_HERE.md`.

---

## 🚀 2부 — 자율 개발(사용자 부재): 안경에 nail-AR 앱 실증
> USB 연결 성공 후, "손톱 검출→디자인 AR 입히는 앱을 안경에 탑재" 목표로 4h 자율 세션. **결과 상세=[docs/GLASSES_APP_RESULTS.md](docs/GLASSES_APP_RESULTS.md).**

- **연결:** RayNeo X3 Pro(ARGF20/AR1/4GB/Android12) 실측 확정.
- **YouTube/웹:** NewPipe + Fennec(Firefox) 안경에 설치·실행(오픈소스, 해시확인).
- **빌드:** 포터블 툴체인(JDK17+Gradle8.9+SDK34, 관리자 불필요)→ 네이티브 nail-AR APK 빌드.
- **앱(안경 검증):** MediaPipe 손톱5개 검출 → **손톱형·곡면·3D음영·손가락별 디자인 오버레이** 렌더. 스크린샷 검증(`samples/app_design_v4*.png`).
- **최종 아키텍처(검증):** 안경 서드파티 **카메라 차단** + 근거리초점 약 → **외부(폰/PC) 캡처 → 안경 검출+표시.** NETWORK MODE로 PC 웹캠 라이브 스트림→안경 렌더 실증(`NET nails · #15`).
- **핵심 플랫폼 발견:** ①카메라 차단(ERROR_CAMERA_DISABLED) ②고정초점 ③디스플레이 idle Doze(깨워야 렌더) ④FLAG_SECURE ⑤Android12 평문차단(usesCleartextTraffic).
- **재현:** `tools/build/run_glasses_demo.ps1 -Mode image|network`.
