opencv-python>=4.9
numpy>=1.24
mediapipe>=0.10.14
# Optional (for the upgrade path, not required to run the MVP):
# ultralytics>=8.3     # YOLO11-seg custom training/inference
# onnxruntime>=1.18    # ONNX inference acceleration / Android export prep
