# 에지 서버(web/edge_serve.py) 실행에 필요한 전체 의존성.
# 노트북(외장 GPU 없어도 OK)에서: python -m pip install -r requirements.txt
opencv-python>=4.9
numpy>=1.24
mediapipe>=0.10.14
cryptography>=42.0     # web/serve.py 자체서명 인증서 자동생성(ensure_cert). 없으면 서버가 안 켜짐.
ultralytics>=8.3       # web/edge_serve.py가 import(YOLO 경로). CUDA 없으면 자동 CPU. torch 자동 포함(~200MB).
# onnxruntime>=1.18    # (선택) ONNX 가속 / 안드로이드 export 준비. 데모엔 불필요.
